__pycache__/
*.pyc
.venv/
venv/
*.db
*.sqlite3
.env
node_modules/
web/dist/
web/*.tsbuildinfo
*.log
.pytest_cache/
.mypy_cache/
.claude/
.idea/
*.apk

# ⛔ MAXFIY: Firebase service account kaliti va shunga o'xshash sirlar.
# Repoga TUSHMASLIGI shart — tushsa kalitni bekor qilib qaytadan yaratish kerak.
# Naqshlar ATAYLAB keng: kalit fayl nomi yuklab olinganda tasodifiy bo'ladi
# (masalan "...-adminsdk-fbsvc-e2bc16ed46.json") va qo'lda qayta nomlanganda
# ".txt" ham bo'lishi mumkin — bittasi o'tib ketsa kalit GitHub'ga chiqadi.
*firebase-adminsdk*
*service-account*.json
*service_account*.json
planning-with-ai*
planning with ai*
fcm*.json
google-services.json
secrets/
*.pem
*.p12

# ⛔ MAXFIY: baza NUSXALARI va zaxiralar — ichida xodimlarning shaxsiy
# ma'lumotlari bor (yuz deskriptorlari, oyliklar, telefon raqamlar).
# DIQQAT: yuqoridagi `*.db` naqshi bularni QAMRAMAYDI, chunki fayl nomi
# ".db" bilan TUGAMAYDI — "app.db.bak_2026-07-27_bosqich1_migration" kabi.
# Repo ochiq (public) bo'lgani uchun bitta `git add -A` butun bazani
# internetga chiqarib yuborardi. Yuz biometrikasini esa parol kabi
# almashtirib bo'lmaydi.
app.db.bak*
*.bak_*
backups/

# Loglar — `*.log` faqat FAYL nomiga mos keladi, papkani qamramaydi
logs/

# Xavfsizlik/bug audit hujjatlari — ichida zaifliklar ro'yxati bo'ladi.
# Ochiq repoda bu hujumchi uchun tayyor qo'llanma.
*bug_audit*
*XAVFSIZLIK_AUDIT*
*TUZATISH_PROMTI*

# ⛔ MAXFIY: Android release imzo parollari. Kalit sizib chiqsa, hujumchi
# XUDDI SHU IMZO bilan zararli APK yasaydi va Android uni haqiqiy ilova
# ustiga "yangilanish" sifatida o'rnatadi (ma'lumot papkasi — jumladan
# SecureStore'dagi JWT — meros qoladi). Parollar `~/.gradle/gradle.properties`
# da turishi kerak; namuna uchun `gradle.properties.example` ga qarang.
mobile/android/gradle.properties
