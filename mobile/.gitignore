# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
# /android — biz android/ni ATAYLAB commit qilamiz: app/build.gradle (release imzo,
# MAX_PATH staging C:/hb) va gradle.properties (ABI, keystore yo'li) qo'lda sozlangan —
# prebuild --clean bularni o'chiradi, ehtiyot bo'ling.

# Firebase FCM kaliti — MAXFIY, repoga tushmasin (build shartli qo'llaydi:
# fayl bo'lmasa APK push'siz quriladi, android/app/build.gradle oxiriga qara)
android/app/google-services.json
